One-line: labelled single-line field — the contact form's building block.

```jsx
<Input label="Email" type="email" placeholder="you@example.com" />
<Input label="Name" error="Required" />
```

There is no red in this palette. Errors are carried by `--accent-text` plus the message itself, never by colour alone.
