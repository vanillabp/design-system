One-line: the fixed back-to-top button — one per page, bottom right.

```jsx
<ScrollToTop iconSrc="assets/icons/scroll-top.svg" />
```

44×44 hit target, fades rather than slides. It is the only fixed element the system defines besides the header.
