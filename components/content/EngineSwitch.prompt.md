One-line: a two-or-three-way pill segmented control; the pressed segment uses the primary button skin.

```jsx
<EngineSwitch options={["Camunda 7", "Camunda 8"]} value={engine} onChange={setEngine} />
```

Keep the option count at two or three — beyond that the row wraps and stops reading as one control.
