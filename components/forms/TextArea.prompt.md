One-line: multi-line field matching Input's shell.

```jsx
<TextArea label="Message" rows={5} placeholder="Tell us everything" />
```

Only vertical resize — horizontal resize breaks the 68ch measure.
